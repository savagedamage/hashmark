# verification keyring — local only, never committed
.gnupg/
# operational state: holds subscriber addresses, stays on this host (never publish PII)
watch/watch.db
*.db-journal
# scratch
*.tmp
__pycache__/
*.pyc
# large downloaded assets never land in git; only digests do
*.tar.gz
*.zip
